{
  "name": "@barter.game/protocol",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "test:deno": "deno test --allow-read $(find test-deno -name '*.deno-test.ts')",
    "typecheck": "tsc --noEmit",
    "build": "echo 'No build step — source is consumed directly as TypeScript modules'"
  },
  "dependencies": {
    "@noble/ed25519": "^3.1.0",
    "@noble/hashes": "^2.2.0",
    "@scure/base": "^2.2.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
